{
  "name": "calculator-app",
  "private": true,
  "scripts": {
    "dev": "concurrently -k \"cross-env PORT=3001 npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3"
  }
}
